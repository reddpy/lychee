import type { JSX } from "react"

/**
 * Context menu stub. LexicalContextMenuPlugin uses a different API (options + menuRenderFn).
 * Add a full implementation later using @lexical/react/LexicalContextMenuPlugin if needed.
 */
export function ContextMenuPlugin(): JSX.Element {
  return null
}
