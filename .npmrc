node-linker = hoisted
# Run pre/post scripts (pretest rebuilds better-sqlite3 for Node; prestart
# rebuilds it for Electron). Without this pnpm skips them and the native module
# ends up built for the wrong runtime.
enable-pre-post-scripts = true
